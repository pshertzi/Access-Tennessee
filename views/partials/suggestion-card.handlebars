<article class="suggestion">
  <div class="title">
    <h4>{{suggestion_text}} </h4>
  </div>
  <div class="meta">
    created by {{user.username}} on
    {{created_at}}
    |
    <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
  </div>
</article>